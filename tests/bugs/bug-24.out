>> option verbose is set to 1
>> inductive type tree was succesfully defined
>> Typing for f, g successful
>> the definition for f is complete
>> the definition for g is complete
>> the definitions f, g are provably correct

val
    f : tree('a,'b) → tree('c,'b)
  | f (Branch⁰ x b) = Branch⁰ x (g b)

val
    g : ('a → tree('a,'b)) → 'c → tree('c,'b)
  | g b x = f ✠

