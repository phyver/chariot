(* ===  functions in environment  =================== *)
val
   k : 'a → 'b → 'a
    | k x y = x

val
   s : ('a → 'b → 'c) → ('a → 'b) → 'a → 'c
    | s x y z = x z (y z)

val
   i : 'a → 'a
    | i = s k k

(* ================================================== *)

