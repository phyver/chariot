(* ===  functions in environment  =================== *)
val
   fst : prod_2('a,'b) → 'a
    | fst (x, y) = x

val
   snd : prod_2('a,'b) → 'b
    | snd (x, y) = y

val
   pair : prod_2(nat,nat)
    | pair = (0, 0)

val
   onetwothree : list(nat)
    | onetwothree = Cons⁷ (1, Cons⁷ (2, Cons⁷ (3, Nil⁷ ())))

(* ================================================== *)

val
   test : prod_3(nat,nat,nat)
    | test = (1, 2, 3)

Fatal error: exception Base.TypeError("cannot infer type of constant Prod_4")
